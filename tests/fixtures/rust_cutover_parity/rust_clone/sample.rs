fn first(value: i32) -> i32 {
    let current = value + 1;
    let doubled = current * 2;
    doubled
}

fn second(item: i32) -> i32 {
    let total = item + 2;
    let twice = total * 2;
    twice
}
